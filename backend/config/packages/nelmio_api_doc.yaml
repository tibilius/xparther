nelmio_api_doc:
    documentation:
        info:
            title: Xparther
            description: App for get dynamic site element by xpath!
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc